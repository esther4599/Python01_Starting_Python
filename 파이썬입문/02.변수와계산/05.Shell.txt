shell
=> 명령을 수행하는 화면

pwd (print working directory)
=> 현재 디렉토리의 위치를 출력

ls(list segment)
=> 현재 디렉토리의 모든 파일을 출력

cd (change directory)
=> 디렉토리 이동
=> .. = 상위 디렉토리를 의미

cp(copy)
=> cp 원본 새파일
=> 파일을 복사

rm(remove)
=> rm 삭제할 파일
=> 파일을 아예 삭제

파일 이름의 두세글자만 입력하고 tab을 누르면 파일 이름이 자동으로 완성됨


